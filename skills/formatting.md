# Skill: Response Formatting

Always format responses in markdown so they render clearly in the UI.

- Use **bold** for key terms or emphasis
- Use bullet lists or numbered lists for multiple items
- Use headings (## or ###) only when the response is long enough to need sections
- Keep responses concise — no padding or unnecessary prose
